name: Release

# Handles npm publishing and Discord for releases.
# Changelog management and tag finalize happen in flow-stable.yml.

on:
  release:
    types: [published]

jobs:
  build_lint_test:
    if: >
      github.event.release.prerelease == false &&
      github.event.release.target_commitish == 'latest' &&
      github.repository == 'ZeliardM/homebridge-kasa-python'
    uses: ./.github/workflows/build-lint-test.yml

  publish:
    needs: build_lint_test
    if: >
      github.event.release.prerelease == false &&
      github.event.release.target_commitish == 'latest' &&
      github.repository == 'ZeliardM/homebridge-kasa-python'
    permissions:
      contents: write
      id-token: write
    uses: ./.github/workflows/npm-publish.yml
    with:
      set_version_from_release_tag: true
      build_cmd: npm run build
    secrets:
      npm_auth_token: ${{ secrets.NPM_TOKEN }}

  release_to_discord:
    name: Discord Webhooks
    needs: [build_lint_test, publish]
    if: >
      github.event.release.prerelease == false &&
      github.event.release.target_commitish == 'latest' &&
      github.repository == 'ZeliardM/homebridge-kasa-python'
    uses: ./.github/workflows/discord-notify.yml
    with:
      title: Kasa Python Release
      username: Homebridge
      avatar_url: https://raw.githubusercontent.com/homebridge/branding/latest/logos/homebridge-color-round-stylized.png
    secrets:
      webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}