---
name: Build, Lint, and Test

on:
  workflow_call: {}

concurrency:
  group: build-lint-test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_lint_test:
    name: Build, Lint, and Test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ${{ fromJson('["22","24"]') }}
        python-version: ${{ fromJson('["3.11","3.12","3.13"]') }}
    steps:
      - name: Checkout Actions
        id: checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Setup Node ${{ matrix.node-version }}
        id: setup-node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Setup Python ${{ matrix.python-version }}
        id: setup-python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install Python Dependencies
        id: install-python-deps
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Install Node Dependencies
        id: install-node-deps
        shell: bash
        run: |
          set -euo pipefail
          npm ci

      - name: Lint
        id: lint
        shell: bash
        run: |
          set -euo pipefail
          npm run lint

      - name: Build
        id: build
        shell: bash
        run: |
          set -euo pipefail
          npm run build

      - name: Node Import Test
        id: node-import-test
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          (async () => {
            try {
              await import('./dist/index.js');
              console.log('Node Import OK');
            } catch (e) {
              console.error(e);
              process.exit(1);
            }
          })();
          NODE

      - name: Python Import Test
        id: python-import-test
        shell: bash
        run: |
          set -euo pipefail
          if [ -f requirements.txt ]; then
            set -e
            while IFS= read -r pkg || [ -n "$pkg" ]; do
              line="$(printf '%s' "$pkg" | sed 's/[[:space:]]//g')"
              [ -z "$line" ] && continue
              case "$line" in \#*) continue ;; esac
              mod="$(printf '%s' "$line" | cut -d'=' -f1 | sed 's/\[.*\]//' | tr '-' '_' )"
              case "$mod" in
                python_kasa) mod="kasa" ;;
              esac
              echo "Import check: $mod"
              python -c "import ${mod}"
            done < requirements.txt
            echo "Python Imports OK"
          fi

      - name: Outdated / Audit
        id: outdated-audit
        shell: bash
        run: |
          set -euo pipefail
          npm list --outdated || true
          npm audit || true
