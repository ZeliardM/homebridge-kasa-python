---
name: Stale

on:
  schedule:
    - cron: '30 1 * * *'

permissions:
      issues: write
      pull-requests: write

jobs:
  stale:
    name: Stale Issues and Pull Requests
    runs-on: ubuntu-latest
    steps:
      - name: Stale
        id: stale
        uses: actions/stale@v10
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          days-before-issue-stale: 60
          days-before-issue-close: 7
          days-before-pr-stale: 30
          days-before-pr-close: 7
          stale-issue-label: stale
          stale-pr-label: stale
          exempt-issue-labels: 'pinned,security,enhancement,bug,breaking-change,needs-info'
          exempt-pr-labels: 'pinned,security,work-in-progress,breaking-change'
          exempt-all-pr-assignees: true
          exempt-all-issue-assignees: true
          operations-per-run: 50
          stale-issue-message: |
            This issue is stale after 60 days of inactivity. It will close in 7 days unless updated.
          close-issue-message: |
            Closing stale issue due to inactivity. Re-open if still valid.
          stale-pr-message: |
            This pull request is stale after 30 days of inactivity. It will close in 7 days unless updated.
          close-pr-message: |
            Closing stale pull request due to inactivity. Re-open if still valid.
