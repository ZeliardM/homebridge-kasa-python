name: Ensure jq
description: Ensure jq is installed (apt-get if missing)
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        if ! command -v jq >/dev/null 2>&1; then
          sudo apt-get update
          sudo apt-get install -y jq
        fi